# ── Core ──
openai>=1.30.0
python-dotenv>=1.0.0

# ── Hardware (Raspberry Pi) ──
# These are only required on the Pi; safely skipped in mock mode on a laptop.
RPi.GPIO>=0.7.1 ; platform_machine == "armv7l" or platform_machine == "aarch64"
hx711>=1.1.2 ; platform_machine == "armv7l" or platform_machine == "aarch64"
picamera2>=0.3.12 ; platform_machine == "armv7l" or platform_machine == "aarch64"

# ── Voice / Audio ──
pyttsx3>=2.90
gTTS>=2.5.0

# ── Notifications ──
twilio>=9.0.0

# ── Image handling ──
Pillow>=10.0.0

# ── Testing ──
pytest>=8.0.0
