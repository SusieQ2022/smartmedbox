# Assets

This folder holds images and demo media.

- `sample_confirmation.jpg` — placeholder image returned by the camera module
  in mock mode (for development without real hardware). Replace with a real
  capture if desired.

Add a team photo and demo screenshots here for the final report.
